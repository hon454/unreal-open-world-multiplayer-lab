# 프로젝트 작업 지침

## 코드 스타일

- `if`·`else`·`for`·`while` 등 제어문의 본문은 한 줄이어도 중괄호를 생략하지 않고, 중괄호와 본문을 별도 줄에 작성한다.
- `if` 블록과 앞뒤 구문 사이는 빈 줄로 구분한다. 바깥 중괄호와 맞닿는 곳이나 `else` 연결부에는 넣지 않는다.

## 아키텍처와 검증

- YAGNI 원칙에 따라 현재 요구·검증에 필요한 최소 구현만 작성한다. 미래 확장용 추상화나 기능은 실제 필요가 생길 때 추가한다.
- 공식 권장 방식과 엔진 기본 기능을 우선하며, 현재 구현의 정확성에 필요한 검증·자원 정리는 유지한다.
- 구현 전에 [아키텍처 안내](Docs/Architecture/README.md)에서 관련 문서를 확인하고, 결정이 바뀌면 해당 문서를 갱신한다.
- 문서는 프로젝트 고유의 결정·제약·미정 사항·검증 기준만 간결하게 기록한다. 일반 기술 설명과 에이전트가 이미 아는 구현 상식, 중복 지침은 적지 않는다.
- 조작 자동화 비용이 큰 검증은 사용자에게 짧은 수동 확인 절차를 요청하고, 응답 전에는 미검증으로 남긴다.

## Unreal Engine 작업

- 반복 배치·일괄 편집·상태 조회 등 자동화 이점이 있는 작업은 Unreal MCP를 우선 사용한다.
- 에셋·폴더 이동, 이름 변경, 리디렉터 해소, 기본 맵 지정처럼 사용자가 에디터에서 몇 번의 클릭으로 끝낼 수 있는 작업은 짧은 수동 절차를 먼저 안내한다. 명시적으로 자동 실행을 요청한 경우에만 직접 조작한다.
- 간단한 작업에서 MCP가 실패하거나 확인 대화상자·미지원 기능에 막히면 같은 호출 재시도, 광범위한 도구 탐색, UI 자동화로 확장하지 않는다. 완료한 부분과 남은 수동 절차만 알리고 해당 에디터 조작을 멈춘다.
- 사용자가 직접 확인·처리하겠다고 하면 추가 조회·스크린샷·검증 호출도 즉시 멈춘다. 사용자가 확인한 결과를 재검증하지 않는다.
- 관련 코드·텍스트 설정·문서 수정은 직접 수행하되, 사용자의 수동 조작이 필요한 단계는 완료로 기록하지 않는다.
- 도구를 호출하기 전에 해당 도구의 입력 스키마를 확인한다.
- MCP가 연결되지 않으면 이를 알리고, 가능한 코드·설정 파일 작업을 진행한다.
- `.uasset` 및 `.umap`은 바이너리를 직접 수정하지 않고 에디터 도구로 편집한다.
- C++ 및 텍스트 설정 파일 수정은 저장소에서 수행하고, 필요한 에디터 검증은 Unreal MCP로 수행한다.
